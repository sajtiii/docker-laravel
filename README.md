# This is a basic Laravel image with nginx

Laravel & php versions are coded into branch names, and pushes to them also trigger the following branches to rebuild them:
- cron
- queue
- octane

These images than can be used to do their specific job
